<!DOCTYPE html>
<html>
<head>
    <!-- ✅ Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
    crossorigin="anonymous"
  />

  <!-- ✅ Bootstrap JS (optional for dropdowns, modals, etc.) -->
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"
  ></script>
  <link rel="stylesheet" href="/dashboard.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css">
  <title>Dashboard</title>
</head>
<body>
  <div class="main-con">
    <div class="side1">
       <span class="cross"><img src="/pics/cross.png" alt="cross" class="img-fluid rounded shadow"></span>
       
      <h1 class="dash"><span class="dot">.</span><i class="fas fa-home me-2"></i>DASHBOARD</h1>
     <a class="nav-link text-white mb-2" href="/medicines">
         <i class="fa-solid fa-prescription-bottle-medical"></i> Medicines
        </a>

     <a class="nav-link text-white mb-2" href="/prescriptions">
          <i class="fas fa-notes-medical me-2"></i> Prescriptions
        </a>

        <form action="/logout" method="POST">
      <a class="nav-link text-danger mt-3" href="/logout">
          <i class="fas fa-sign-out-alt me-2"></i> Logout
        </a>
    </div>
    <div class="side2">
      <h1>Welcome, {{message.username}}</h1>
      <p>Your role: {{message.role}}</p>
      <p>Your email: {{message.email}}</p>
{{#ifEquals message.role "doctor"}}
  <form action="/prescriptions" method="POST" class="mt-2">
    <select name="patientId" required class="form-control mb-1">
      {{#each patients}}
        <option value="{{this._id}}">{{this.username}} ({{this.email}})</option>
      {{/each}}
    </select>
  </form>
{{/ifEquals}}
    </div>
    <div class="image-main">
       <span class="doc"><img src="/pics/Clear background med.png" alt="cross" ></span>
       <span class="med"><img src="/pics/Medical items illust.png" alt="cross" ></span>
    </div>
  </div>
</body>
</html>